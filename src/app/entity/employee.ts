export interface Employee {

//Do Interface this class for the data
//need to put string but doesn't take, so pu any-----
  employeeID?:any; 
  firstName?: string;
  lastName?:string;
  address?:string;
  gender?: string;
  dob?:string;
  phone? : string;
  email? : string;
  active? : string;
  status? :string;
}
